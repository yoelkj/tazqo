---
import LandingLayout from "../layouts/LandingLayout.astro";
import Container from "../components/Container.astro";
import CTAButton from "../components/CTAButton.astro";
import SectionTitle from "../components/SectionTitle.astro";
//import Pill from "../components/Pill.astro";
import Pricing from "../components/Pricing.astro";
import Expertise from "../components/Expertise.astro";
import HowItWorks from "../components/HowItWorks.astro";
import FAQ from "../components/FAQ.astro";

// Si tu config está en la raíz, usa "../../config"

import { BRAND, CONTACT, TAX } from "../config";

/* ✅ Chips del hero: texto, color del punto y posición absoluta */
const heroChips = [
  {
    text: "Confirmación de pedido",
    dot: "bg-emerald-500",
    class: "top-20 -right-4",
  },
  {
    text: "Reporte diario a WhatsApp",
    dot: "bg-indigo-500",
    class: "bottom-28 -left-6",
  },
  { text: "Lead capturado", dot: "bg-amber-500", class: "top-40 -left-10" },
  { text: "Factura emitida", dot: "bg-rose-500", class: "bottom-10 right-0" },
  {
    text: "Cita confirmada",
    dot: "bg-sky-500",
    class: "top-6 left-1/2 -translate-x-1/2",
  },
  {
    text: "Reseña solicitada",
    dot: "bg-teal-500",
    class: "bottom-1/3 -right-8",
  },
];
---

<LandingLayout>
  <!-- HERO -->
  <section id="hero" class="relative hero-bg -mt-16 pt-16 border-b">
    <div class="absolute inset-0 bg-grid opacity-35 pointer-events-none"></div>
    <Container class="relative py-24 md:py-28">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <!-- copy -->
        <div>
          <p class="text-sm uppercase tracking-wide text-slate-500">
            — Automatización confiable para MYPEs
          </p>
          <h1 class="h1 mt-3">
            Operaciones sin fricción.
            <span class="text-brand-indigo">Tus sistemas, solos.</span>
          </h1>
          <p class="measure mt-5 text-[1.05rem] md:text-xl text-slate-600">
            Conectamos WhatsApp, Google Sheets, Gmail, Shopify y más para que
            pedidos, reportes, citas y facturación se ejecuten automáticamente.
            Entregables en días, con observabilidad y control.
          </p>
          <div class="mt-8 flex flex-col sm:flex-row gap-4">
            <CTAButton
              target="_blank"
              href={CONTACT.whatsappUrl}
              text="Quiero automatizar mi proceso"
            />
            <a
              href="#como"
              class="inline-block px-6 py-3 rounded-xl border border-slate-200 font-semibold hover:bg-slate-50"
            >
              Ver cómo trabajamos
            </a>
          </div>
          <div class="mt-8 flex flex-wrap gap-2">
            <span class="pill">Idempotencia</span>
            <span class="pill">Reintentos con backoff</span>
            <span class="pill">Observabilidad</span>
            <span class="pill">Documentación</span>
          </div>
        </div>

        <!-- mockup + chips -->
        <div class="relative">
          <img
            src="/img/device-mock.svg"
            alt={`${BRAND.name}: flujos automatizados`}
            class="w-[92%] max-w-[420px] mx-auto"
          />

          {
            heroChips.map((c) => (
              <div
                class={`hidden sm:flex items-center gap-2 absolute ${c.class} surface px-3 py-2 text-sm shadow-elev-1 pointer-events-none`}
              >
                <span class={`h-2.5 w-2.5 rounded-full ${c.dot}`} />
                {c.text}
              </div>
            ))
          }
        </div>
      </div>
    </Container>
  </section>

  <!-- SERVICIOS -->
  <section id="servicios" class="bg-white">
    <Container class="py-20 md:py-24 text-center">
      <SectionTitle
        align="center"
        kicker="Servicios"
        title="Flujos que impactan tu operación"
        subtitle="Construimos y monitoreamos automatizaciones sobre tus herramientas actuales."
      />
      <div class="mt-12 grid sm:grid-cols-2 lg:grid-cols-4 gap-6 text-left">
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              ↗
            </div>
            <div>
              <div class="card-title">Pedidos & ventas</div>
              <p class="card-desc">
                De WhatsApp/tienda a Sheets o ERP, con estado y confirmaciones.
              </p>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              ◎
            </div>
            <div>
              <div class="card-title">Reportes diarios</div>
              <p class="card-desc">
                Totales y variaciones a WhatsApp/Email en tu horario.
              </p>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              ✓
            </div>
            <div>
              <div class="card-title">Citas & recordatorios</div>
              <p class="card-desc">
                Confirmaciones y reprogramación automática para reducir
                no-shows.
              </p>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              ▣
            </div>
            <div>
              <div class="card-title">Facturas & cotizaciones</div>
              <p class="card-desc">
                PDF con tu marca y envío automático por email/WhatsApp.
              </p>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              ★
            </div>
            <div>
              <div class="card-title">Reseñas post-compra</div>
              <p class="card-desc">
                Solicitud de opiniones con enlaces a Google/Facebook.
              </p>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              ⇆
            </div>
            <div>
              <div class="card-title">Leads de anuncios</div>
              <p class="card-desc">
                Sincronización desde Meta/Google Ads a CRM o Sheets.
              </p>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              ⚙
            </div>
            <div>
              <div class="card-title">Integraciones</div>
              <p class="card-desc">
                WhatsApp Cloud API, Apps Script, Make, n8n, Zapier, Shopify,
                WooCommerce.
              </p>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="flex items-start gap-3">
            <div
              class="h-10 w-10 rounded-full bg-slate-100 text-slate-700 flex items-center justify-center"
            >
              🔔
            </div>
            <div>
              <div class="card-title">Alertas & monitoreo</div>
              <p class="card-desc">
                Logs, trazabilidad y avisos ante fallos; reintentos con backoff.
              </p>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-10">
        <CTAButton
          target="_blank"
          href={CONTACT.whatsappUrl}
          text="Quiero este tipo de automatizaciones"
        />
      </div>
    </Container>
  </section>

  <!-- EXPERTISE -->
  <Expertise />

  <!-- CÓMO TRABAJAMOS -->
  <HowItWorks />

  <!-- PRECIOS -->
  <Pricing showAddons={true} />

  <!-- FAQ -->
  <FAQ />

  <!-- CTA FINAL -->
  <section id="contacto" class="bg-slate-950 text-white">
    <Container class="py-20 md:py-24 text-center">
      <h2 class="h2 text-white">Listo para eliminar tareas manuales</h2>
      <p class="mt-3 text-slate-300">
        Cuéntanos tu proceso y te proponemos el primer flujo en días.
      </p>
      <div class="mt-6">
        <CTAButton
          target="_blank"
          href={CONTACT.whatsappUrl}
          text="Automatiza mi negocio"
        />
      </div>
    </Container>
  </section>
</LandingLayout>
